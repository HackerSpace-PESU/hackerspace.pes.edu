setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    website: Dockerfile
# release:
#   image: website
#   command: 
#     - ./pre-deploy.sh
run:
  website: gunicorn --bind 0.0.0.0:$PORT 'hackerspace_site.wsgi.py:application'
