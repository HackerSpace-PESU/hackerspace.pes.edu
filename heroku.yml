setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE

build:
  docker:
    web: Dockerfile

release:
  command: 
    - ./pre-deploy.sh
  image: web

run:
  web: gunicorn --bind 0.0.0.0:$PORT 'hackerspace_site.wsgi.py:application'
